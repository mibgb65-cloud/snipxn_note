-- ============================================================
-- V1  初始化：启用 UUID 扩展
-- 整个数据库只需执行一次，后续所有表的主键默认值依赖此扩展
-- ============================================================

CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
